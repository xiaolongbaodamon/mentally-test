import React, { useState } from "react";
export const SleepHabitsTracker: React.FC = () => { const [hours, setHours] = useState("7"); const [saved, setSaved] = useState(false); return <div className="rounded-2xl border border-slate-200 bg-white p-6"><h3 className="text-lg font-bold text-slate-900">Sleep and habits</h3><p className="mt-1 text-xs text-slate-500">Track one small recovery habit for today.</p><label className="mt-6 block text-xs font-bold text-slate-700">Hours of sleep<input type="number" min="0" max="24" value={hours} onChange={(event) => setHours(event.target.value)} className="mt-2 block w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm" /></label><button onClick={() => { localStorage.setItem("mentally_habits", JSON.stringify({ date: new Date().toISOString().slice(0, 10), hours })); setSaved(true); }} className="mt-5 rounded-xl bg-teal-600 px-4 py-2.5 text-xs font-bold text-white">{saved ? "Saved for today" : "Save today's check-in"}</button></div>; };
