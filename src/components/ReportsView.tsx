import React from "react";
import type { JournalEntry, MoodEntry } from "../types";
interface ReportsViewProps { moods: MoodEntry[]; journals: JournalEntry[]; streakDays: number; activitiesCompletedCount: number; }
export const ReportsView: React.FC<ReportsViewProps> = ({ moods, journals, streakDays, activitiesCompletedCount }) => { const average = moods.length ? (moods.reduce((sum, mood) => sum + mood.score, 0) / moods.length).toFixed(1) : "-"; return <div className="space-y-5"><div><h3 className="text-lg font-bold text-slate-900">Wellness report</h3><p className="text-xs text-slate-500">A simple view of your recorded self-care activity.</p></div><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{[["Average mood", average], ["Mood logs", moods.length], ["Journal entries", journals.length], ["Activities", activitiesCompletedCount]].map(([label, value]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-2xl font-black text-teal-700">{value}</p></div>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-5 text-sm text-slate-700">Current check-in streak: <strong>{streakDays} day{streakDays === 1 ? "" : "s"}</strong></div></div>; };
