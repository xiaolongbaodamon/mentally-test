import React, { useState } from "react";
import { Brain, CheckCircle2, Wind } from "lucide-react";

interface SelfCareActivitiesProps { onActivityCompleted: (name: string) => void; defaultSubTab?: "breathing" | "grounding" | "ambient" | "articles"; }
const activities = { breathing: { title: "Box Breathing", text: "Inhale for four counts, hold for four, exhale for four, and rest for four." }, grounding: { title: "5-4-3-2-1 Grounding", text: "Name five things you see, four you feel, three you hear, two you smell, and one you taste." }, ambient: { title: "Quiet Reset", text: "Lower your shoulders, soften your gaze, and take three comfortable breaths." }, articles: { title: "A mindful pause", text: "A short pause can help your attention return to the next manageable step." } };
export const SelfCareActivities: React.FC<SelfCareActivitiesProps> = ({ onActivityCompleted, defaultSubTab = "breathing" }) => { const [tab, setTab] = useState(defaultSubTab); const item = activities[tab]; return <div className="space-y-5"><div className="flex gap-2 overflow-x-auto">{Object.keys(activities).map((key) => <button key={key} onClick={() => setTab(key as typeof tab)} className={`rounded-xl px-3 py-2 text-xs font-bold ${tab === key ? "bg-teal-600 text-white" : "bg-white text-slate-600 border border-slate-200"}`}>{key[0].toUpperCase() + key.slice(1)}</button>)}</div><section className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-3"><div className="rounded-xl bg-teal-50 p-3 text-teal-700"><Wind className="h-5 w-5" /></div><div><h3 className="font-bold text-slate-900">{item.title}</h3><p className="text-xs text-slate-500">A brief self-care practice for your next reset.</p></div></div><p className="mt-6 rounded-xl bg-slate-50 p-5 text-sm leading-relaxed text-slate-700">{item.text}</p><button onClick={() => onActivityCompleted(item.title)} className="mt-5 flex items-center gap-2 rounded-xl bg-teal-600 px-4 py-2.5 text-xs font-bold text-white"><CheckCircle2 className="h-4 w-4" /> Mark complete</button></section></div>; };
